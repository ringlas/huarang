# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page

GET     /                                   controllers.Application.home()
GET     /login                              controllers.Application.login()
POST    /login                              controllers.Application.authenticate()
POST    /add                                controllers.Application.createUser()

# Gamebook pages

GET     /skills                             controllers.Gamebook.chooseSkill()
GET     /skillsbonus                        controllers.Gamebook.skillBonus()
GET     /display/:number                    controllers.Gamebook.displayEpisode(number: Integer)
GET     /gamebook/generate/:number          controllers.Gamebook.generateEpisode(number: Integer)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file                       controllers.Assets.at(path="/public", file)
